#include<stdio.h>
#include<math.h>
main()
{
	char c;
	int i;
	char num1[65],num2[65],soma[65],v1=1;
	printf("Digite o primeiro operando: \n");
	scanf("%s",&num1);
	printf("Digite o primeiro operando: \n");
	scanf("%s",&num2);
	num1[64]='\0';
	num2[64]='\0';
	for (i=0;i<=64;i++)
	{
		if (num1[i]==' ')
			num1[i]=0;
		if (num2[i]==' ')
			num2[i]=0;	
		if (num1[i]+num2[i]==0||num1[i]+num2[i]==1)
			soma[i]=num1[i]+num2[i];
		else
			soma[i]=1;			
			if (num[i+1]==0)
				num1[i+1]+=v1;
			else
				num2[i+1]+=v1;
	}
	printf("Soma= %s",soma);
}
